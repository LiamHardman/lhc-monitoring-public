for fresh install without config
helm upgrade --install loki --namespace=monitoring grafana/loki-stack


for test of new config
helm upgrade -f values.yml loki --namespace=monitoring grafana/loki-stack --dry-run --debug



To upgrade with new config
helm upgrade --install -f values.yml loki --namespace=monitoring grafana/loki-stack